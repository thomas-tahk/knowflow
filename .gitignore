# brainstorm companion sessions (mockups, server state)
.superpowers/

node_modules/
dist/
.env
.env.*
.DS_Store

# worktrees created by Claude Code — full checkouts, never repo content
.claude/worktrees/
